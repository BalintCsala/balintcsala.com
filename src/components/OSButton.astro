---
import Box from "./Box.astro";

interface Props {
    id: string;
}

const { id } = Astro.props;
---

<button
    class="active:debossed embossed-button bg-silver h-5 shrink-0 items-center text-base text-black"
    id={id}
>
    <slot />
</button>
